"""CLI application for a prefix-notation calculator."""

from arithmetic import (add, subtract, multiply, divide, square, cube,
                        power, mod, )


# Replace this with your code
"""
have 3 parameters as inputs 
    operand
    num1
    num2

tokenize based on spaces ' '
if it's just one parameter "q" it quits the program

after we split it we write our funtions for each of the conditional statements.
"""
initial_string = input(f"Enter prefix calculation: ")

